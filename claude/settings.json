{
  "attribution": {
    "commit": "",
    "pr": "",
    "sessionUrl": false
  },
  "permissions": {
    "defaultMode": "auto"
  },
  "model": "claude-fable-5-1[1m]",
  "statusLine": {
    "type": "command",
    "command": "~/.claude/statusline.sh",
    "padding": 0
  },
  "effortLevel": "medium",
  "modelSettings": {
    "claude-opus-4-8": {
      "effortLevel": "medium"
    },
    "claude-fable-5-1": {
      "effortLevel": "low"
    },
    "claude-sonnet-5": {
      "effortLevel": "high"
    },
    "claude-opus-5": {
      "effortLevel": "low"
    }
  },
  "autoCompactWindow": 600000,
  "theme": "dark",
  "autoCompactEnabled": true,
  "remoteControlAtStartup": true,
  "inputNeededNotifEnabled": true,
  "agentPushNotifEnabled": true,
  "autoMode": {
    "soft_deny": [
      "$defaults",
      "Bash(Game/tools/gamebuild:*) hand-editing the generated gamebuild bootstrap file in pixeption/nono4u — CLAUDE.md states it must never be hand-edited, only re-synced via Unity's Sync CLI Bootstrap"
    ],
    "environment": [
      "### Org-wide",
      "**Organization**: None configured",
      "**Cloud provider(s)**: None configured",
      "**Repository visibility**: Public — pixeption/dotfiles (github.com:pixeption/dotfiles.git)",
      "**Internal sharing / snippet hosting**: None configured — treat public paste/gist services as outside the trust boundary",
      "**Secrets management**: None configured",
      "**Default / protected branches**: Default branch main; no rulesets or protected branches listed for pixeption/dotfiles",
      "**CI/CD deploy targets**: None configured",
      "**Network posture**: None configured",
      "**Source control**: The trusted repo (pixeption/dotfiles) and its remote(s) only (no additional orgs configured)",
      "**Trusted internal domains**: None configured",
      "**Trusted cloud buckets**: None configured",
      "**Key internal services**: None configured",
      "**Internal package registry**: None configured",
      "**Sensitive data locations & audiences**: any file or store holding personal data, confidential business data, credentials, regulated data, or similarly sensitive material; preserve exact handles when known and share only with audiences cleared at the [named+specifics] bar",
      "**Data retention / declassification**: None configured",
      "**Sensitive remote targets**: any namespace, host, or container whose name carries `prod` or `production` as a whole word or name segment (hyphen/underscore/dot-delimited)",
      "**Protected deployment namespaces / environments**: None configured — fall back to the Sensitive remote targets heuristic",
      "**Protected IaC scopes**: IAM, RBAC, networking, quota, and node-pool resources; anything whose name or tag carries `prod` or `production` as a whole word or name segment",
      "**Organization**: pixeption (github.com/pixeption)",
      "**Repository visibility**: Private — pixeption/nono4u (github.com:pixeption/nono4u.git)",
      "**Default / protected branches**: Default branch main; no rulesets queryable here, no protected branches listed for pixeption/nono4u — treat as unprotected only with caution since rulesets could not be queried",
      "**Source control**: The trusted repo (pixeption/nono4u) and its remote(s) only (no additional orgs configured)",
      "**Sensitive data locations & audiences**: any file or store holding personal data, confidential business data, credentials, regulated data, or similarly sensitive material; preserve exact handles when known and share only with audiences cleared at the [named+specifics] bar — note: .gitignore flags Game/tools/build/*.keystore and /build.env, /.env.local plus several unusually-named patterns as sensitive-looking, keep these out of commits/pushes",
      "**Sensitive remote targets**: any namespace, host, or container whose name carries `prod` or `production` as a whole word or name segment (hyphen/underscore/dot-delimited — e.g. matches `prod-db`, not `producer`)",
      "### User-specific",
      "**Primary use of Claude Code**: software development (dotfiles / personal config management)",
      "**Trusted repo**: pixeption/dotfiles (public — its own committed dotfiles content is OK to push there; content ported or first read from outside this repo is not its own work and should not be pushed here without confirmation; secrets and sensitive data are never cleared into this repo by its public visibility)",
      "**Org-specific CLIs**: None configured",
      "routine actions under this repo's own working directory (~/.config) are treated as local/trusted for edits and reads",
      "**Primary use of Claude Code**: software development (Unity game + .NET level-authoring tool monorepo)",
      "**Trusted repo**: The git repository the agent started in (/Users/vbnn2/code/nono4u, pixeption/nono4u — private) and its configured remote(s); confidential material is fine here, but content ported, repointed, or first read from outside this session's repo is not its own work and should not be pushed without confirmation; secrets and sensitive data are never cleared into this repo by its private visibility",
      "**Org-specific CLIs**: unity, dotnet, tools/gen-backlog.sh, tools/check-docs.sh, Game/tools/gamebuild — routine under this repo's own working directory",
      "routine actions under /Users/vbnn2/code/nono4u are treated as local/trusted for edits and reads"
    ]
  }
}
